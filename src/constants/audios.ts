// src/constants.ts

export const AUDIOS: { [key: string]: string } = {
	enter: "enter",
};

export const AUDIO_ACTIONS: { [key: string]: string } = {
	play: "play",
	stop: "stop",
	pause: "pause",
};
